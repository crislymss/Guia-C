#include <stdio.h>

void main(){
    char texto[10];
    char c;
    int i;
    puts("Entre com o texto:");
    gets(texto);
    puts("Texto digitado:");
    puts(texto);
//    i = 0;
//    while( (texto[i] != '\0') && (i<10) ){
//        putchar(texto[i]);
//        putchar('\n');
//        i++;
//    }
}
